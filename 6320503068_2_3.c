#include<stdio.h>

    void main()
    {
        int num[3],i;
        for( i=0 ; i<3 ; i++ )
        {
            scanf("%d",&num[i]);
        }
        for( i=0 ; i<6 ; i++ )
        {
            if(num[i]>num[i+1])
            {
               token=num[i+1];
               num[i+1]=numi[];
            }
        }
    }
